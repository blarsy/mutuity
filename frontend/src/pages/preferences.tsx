import { PlaceholderPage } from "../features/layout/PlaceholderPage";

export default function PreferencesPage() {
  return (
    <PlaceholderPage
      title="Preferences"
      description="This page will let the logged-in account adjust notification and application preferences."
    />
  );
}
