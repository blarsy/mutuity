# DB Layer

This folder is reserved for backend-side DB access helpers.
In the PostGraphile-first architecture, domain writes and reads are primarily enforced in PostgreSQL (schema, functions, grants, RLS).
