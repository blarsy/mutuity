# Auth Layer

This folder contains backend authentication helpers that are not directly modeled in PostgreSQL.
Role checks for business actions should remain enforceable at the database layer through grants and RLS.
